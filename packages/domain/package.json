{
  "name": "@timsan/domain",
  "version": "0.0.1",
  "private": true,
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:pbt": "vitest run --reporter=verbose"
  },
  "dependencies": {
    "@timsan/shared": "workspace:*"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "2.1.8",
    "fast-check": "3.23.2",
    "typescript": "5.7.2",
    "vitest": "2.1.8"
  }
}
