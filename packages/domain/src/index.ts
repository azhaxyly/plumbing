/**
 * @whitehouse/domain
 * Pure domain logic — no Next.js, no I/O, no side effects.
 * Contains: types, cart logic, order state machine, shipping calculator, etc.
 */

// Types will be exported here as they are implemented in subsequent tasks
export type { Currency, Money } from "./types";
